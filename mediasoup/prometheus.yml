global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'mediasoup-signaling'
    static_configs:
      - targets: ['host.docker.internal:3000']
    scheme: https
    tls_config:
      insecure_skip_verify: true